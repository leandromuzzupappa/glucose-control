import { Use{{pascalCase name}}ReturnType } from "./{{pascalCase name}}.types";

export const use{{pascalCase name}} = (): Use{{pascalCase name}}ReturnType => {
  const foo = "bar";

  return { foo };
};
